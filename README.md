# playground-agent

## Streamlit 채팅 (GPT 스타일)

```bash
python -m venv .venv && source .venv/bin/activate
pip install -e .
cp .env.example .env   # OPENAI_API_KEY 설정
streamlit run streamlit_app.py
```
